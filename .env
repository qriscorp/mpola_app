# Mpola Mobile App
# ─────────────────────────────────────────
# Points at the real deployed backend (same one the website uses) so
# sign-in/data match what's already live — not a local/emulator-only address.
EXPO_PUBLIC_API_URL=https://api.welend.qriscorp.com
